{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./back-auth/bin/www",
    "heroku-postbuild": "cd back-auth && npm install && cd ../front-end-auth && npm install && npm run build"
  }
}
